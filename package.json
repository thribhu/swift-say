{
  "name": "milana",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "npx lerna run lint",
    "format": "npx lerna run format",
    "serve-local": "npx lerna run dev"
  },
  "engines": {
    "yarn":"1.22.19"
  },
  "dependencies": {}
}
